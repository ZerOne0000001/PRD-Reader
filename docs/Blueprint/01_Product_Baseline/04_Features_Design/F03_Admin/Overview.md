# ⚙️ 功能概述：后台配置 (F03_Admin)

## 1. 业务目标
为系统管理员提供一个安全、便捷的配置控制台。管理员在此处配置底层代码托管平台（GitLab/GitHub）的鉴权信息和需要对外暴露的仓库白名单。这是整个 PRD-Reader 系统的“引擎室”。

## 2. 核心价值
*   **权限收口**：将所有敏感的 Token 集中在后端代理层配置，前端用户实现真正的“零鉴权”访问。
*   **灵活接入**：支持多平台（目前支持 GitLab 和 GitHub）的动态切换和独立配置。
*   **即时生效**：白名单的增删查改能够立即作用于前台用户的文件树展示。